/**
 * @author baramofme
 *
 * 기타(etc) 타입
 *
 */
// 두 타입의 모든 속성을 가져야 한다.
let birdCat = { leg: 4, wing: 2 };
/*
* 유니언
*
* 2개 이상의 타입을 하나의 타입으로 정의한 타입
* */
// 두 타입 중 하나의 속성만 가져도 된다.
let birdOrCat = { leg: 4 };
let uniStringNumber;
